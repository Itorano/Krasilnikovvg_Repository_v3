#include <iostream>
using namespace std;

int Input_Number(int Number) {
    cout << "1. ¬ведите натуральное число X: ";
    cin >> Number;
    return Number;
}

int Input_Digit(int Digit) {
    cout << "2. ¬ведите цифру A: ";
    cin >> Digit;
    return Digit;
}

bool Digit_Presence(int Number, int Digit) {
    while (Number > 0) {
        int lastDigit = Number % 10;
        if (lastDigit == Digit) {
            return false;
        }
        Number /= 10;
    }
    return true;
}

int main() {
    int X, A, Command;

    do
    {
        cout << "¬ведите номер команды:";
        cin >> Command;
        
        switch (Command) {
            case 1: 
                X = Input_Number(X); 
                break;
            case 2: 
                A = Input_Digit(A);
                break;
            case 3:
                if (Digit_Presence(X, A)) {
                    cout << "3. ÷ифра " << A << " отсутствует в числе " << X << endl;
                } else {
                    cout << "3. ÷ифра " << A << " присутствует в числе " << X << endl;
                }
                break;
            case 4:
                if (Digits_More_Than_Twice(X, A)) {
                    cout << "4. ÷ифра " << A << " встречаетс€ более двух раз в числе " << X << endl;
                } else {
                    cout << "4. ÷ифра " << A << " встречаетс€ не более двух раз в числе " << X << endl;
                }
                break;
        }
    } while(Command > 0 && Command < 5);

    return 0;
}